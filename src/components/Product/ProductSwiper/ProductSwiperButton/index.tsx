"use client";

import styled from "styled-components";

const StyledSwiperButton = styled.button`
	display: block;

	width: 40px;
	height: 40px;
`;

const ProductSwiperButton = () => {
	return <StyledSwiperButton />;
};

export default ProductSwiperButton;
